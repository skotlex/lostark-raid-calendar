# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 자동으로 읽는 컨텍스트다.
작업 전 전체를 읽고, "규칙" 섹션을 위반하지 않는다.

---

## 1. 프로젝트

로스트아크 길드 레이드 편성표. 요일별 레이드 슬롯에 길드원이 캐릭터를 배치하고,
클래스·템레벨·전투력·각인·아크그리드를 로아 공식 OpenAPI로 자동 표시한다.

기존에 쓰던 Google 시트를 대체한다. 시트는 캐릭터 스펙 참조가 `#REF!`로 깨져 있었고,
매주 인원을 손으로 지워야 했으며, 셀 편집이라 남의 신청을 실수로 지우기 쉬웠다.
**이 세 가지를 고치는 것이 이 앱의 존재 이유다.**

**사용자: 길드원 다수.** 링크를 공유해 함께 편집한다.

### 범위 밖

- **Gmail 확장 프로그램**: 이 저장소의 이전 목적이었다. 2026-09-04에 전면 폐기했다.
  관련 코드·문서·OAuth 설정을 되살리지 않는다
- **개인 계정 로그인**: 의도적으로 만들지 않는다. 섹션 4 참조
- **시트 임포트**: 기존 시트에서 데이터를 가져오지 않는다. 처음부터 새로 입력한다

---

## 2. 핵심 설계 — 슬롯과 배정의 분리

이 앱에서 가장 먼저 이해해야 할 구조다.

| 모델 | 성격 | 리셋 |
|---|---|---|
| `RaidSlot` | 고정 요일표의 한 칸. 요일·시간·레이드·난이도·인원 | 안 됨. 영속적 |
| `Assignment` | 그 칸에 누가 들어갔는지. `weekStart`를 가짐 | 매주 |

**리셋은 크론이나 배치 삭제가 아니다.** 조회할 때 현재 주차의 배정만 읽으므로 새 주차가
시작되면 자동으로 빈 편성이 된다. 지난 주 기록은 남는다. 이 구조를 깨는 방향
(예: 매주 Assignment를 DELETE하는 크론)으로 바꾸지 않는다.

**주차 경계는 게임과 같은 KST 수요일 06:00**이다. 다만 **비워지는 시각이 요일에 따라
갈린다.**

| 슬롯 요일 | 비워지는 때 | 쓰는 함수 |
|---|---|---|
| 수 · 목 · 금 · 토 · 일 · 월 · 미정 | **화요일 00시** (주차 경계보다 30시간 이르다) | `getPlanningWeekStart()` |
| 화 | 수요일 06시 (주차 경계 그대로) | `getWeekStart()` |

화요일 밤 레이드는 주차가 갈리기 직전에 열린다. 수~월과 함께 화요일 00시에 비우면 그날
저녁 공대가 몇 시간 전에 지워진다. 반대로 수~월을 주차 경계까지 들고 있으면 다음 주
편성을 짤 시간이 없다. 그래서 앞의 여섯 요일만 30시간 먼저 비운다.

화면이 보는 주차는 `getPlanningWeekStart()`이고, 화요일 슬롯의 주차는 거기서
`tuesdayWeekFor()`로 구한다. 둘은 **화요일 00시부터 수요일 06시까지 30시간만 어긋난다.**
슬롯 하나에 맞는 값은 `weekStartForDay(주차, 요일)`가 준다. **DB에 쓰거나 읽을 때는
반드시 이 함수를 거친다.** 화면이 넘겨준 주차를 그대로 쓰면 화요일 칸에 넣은 사람이
다음 주 칸에 들어간다.

`src/lib/week.ts`가 유일한 정답지이고 유닛 테스트가 붙어 있다.
이 정의를 다른 곳에서 다시 구현하지 않는다.

### 요일 하나가 더 있다 — 미정

**요일 목록의 화요일 다음이 `미정`(`UNDECIDED = 7`)이다.** 사람을 모으지 않고 혼자 도는
레이드를 위한 칸이라 요일도 시각도 정할 것이 없다. 그런데 **숙제는 편성표에서 나오므로**
(2-3) 편성표에 넣지 않으면 숙제 화면에도 뜨지 않는다. 그래서 요일 자리에 하나를 더 둔다.

0~6 다음 번호라 `RaidSlot.dayOfWeek`(Int)에 그대로 들어간다. 새 컬럼도 마이그레이션도
필요 없다. 화요일이 아니므로 `weekStartForDay`가 수~월과 같은 주차를 주고, 승계도
같은 무리에서 돈다(`carryOverGroup`의 `{ not: TUESDAY }`).

| | |
|---|---|
| 시각 | 폼에서 칸을 **잠그고** `-`를 박는다. 저장은 `00:00`으로 몰아둔다 |
| 목록 | 요일표 편집의 미정 무리는 요일·시각 두 칸 모두 `-`다. 어느 무리인지는 제목이 말한다 |
| 편성표 | 요일 탭 줄이 아니라 **반대쪽 끝, 보기 토글 왼쪽**이다 |
| 숙제 | `done`이 끝까지 false다. 지났는지 잴 시각이 없다. 주차가 넘어갈 때 함께 사라진다 |

시각 칸을 **감추지 않고 잠그는** 이유는 감추면 옆 칸들이 밀려 폼이 요일마다 다른 모양이
되기 때문이다. 같은 이유로 목록에서도 칸을 지우지 않고 `-`를 남긴다.

편성표에서 미정 탭을 요일 줄에 붙이지 않는다. 그 줄은 수→화로 주간 일정을 훑는
자리라 끝에 붙이면 "화요일 다음"으로 읽힌다. 탭 모양은 그대로 두되(같은 종류의 칸이다)
자리만 떼어 놓는다. 밑줄이 레일에 닿아야 하므로 오른쪽 묶음(`pb-1.5`) 안이 아니라
레일의 직계 자식으로 둔다.

`dayOffsetInWeek()`이 미정에 `-1`을 준다. 요일 순서(`WEEK_DAYS`)를 그대로
`indexOf`하면 미정이 7번째 날로 잡혀 숙제가 엉뚱한 시각에 "다녀온 것"이 된다.

### 리셋 예외는 두 단위

| 단위 | 필드 | 용도 |
|---|---|---|
| 자리 | `Assignment.pinned` | 고정 6명 + 유동 2자리 |
| 레이드 | `RaidSlot.keepRoster` | 슬롯 전체가 고정 공대 |

승계 규칙: **새 주차 첫 조회 시, 직전 주차 배정 중 `slot.keepRoster`가 켜져 있거나
`pinned`가 켜진 자리를 복사한다.**

**두 값은 함께 움직인다.** 전원 고정을 켜면 그 주 인원 전체에 핀이 꽂히고, 끄면 모두
빠진다. 그러지 않으면 "전원 고정"인데 칸에는 아무 표시가 없어 무엇이 넘어가는지 알 수
없다. 반대로 자리 하나를 풀면 전원 고정이 꺼진다. 나머지 핀은 그대로 둔다 — 방금 한 일은
"한 명을 뺀 것"이지 "고정을 전부 푼 것"이 아니다. lazy carry-over이며 unique 제약 덕분에 중복 실행에
안전하다.

---

## 2-1. 8인 = 4인 파티 둘

편성은 `딜1 딜2 딜3 폿1` / `딜4 딜5 딜6 폿2` 두 파티로 나뉜다. 화면 배치만의 문제가 아니라
**시너지가 4인 파티 단위로 적용**되기 때문이다. 8인을 한 덩어리로 계산하면 실제 게임과
어긋난다. `positions.ts`의 `PARTIES`가 유일한 정의다.

**4인 레이드도 있다.** 세르카·지평의 성당처럼 4인으로 가는 레이드는 **1파티만** 쓴다.
`RaidSlot.partySize`(4 또는 8)가 정하고, 자리 이름은 8인의 1파티를 그대로 쓴다
(`DPS1~3`, `SUP1`). 따로 두면 인원을 바꿀 때 앞 넷까지 날아간다.

인원은 레이드가 정하는 값이라 **고르는 칸을 두지 않는다.** 저장할 때 레이드 이름으로
정한다(`raids.ts`의 `size`, `slots.ts`의 `normalize`). 프리셋에 없는 이름은 8인이다.
그래서 4인 레이드가 새로 나오면 프리셋에 넣어야 한다. 폼에서는 못 바꾼다.

`partiesFor(size)`가 유일한 정의다. `PARTIES`를 직접 순회하면 4인 슬롯에 2파티가 생긴다.
같은 이유로 자리 검증(`isValidPosition`)에도 슬롯의 인원을 함께 넘긴다. 넘기지 않으면
4인 슬롯에 `DPS5`가 들어가 화면에 나오지 않는 유령 배정이 된다.

시너지 요약은 **없는 것이 아니라 들어온 것**을 파티별로 보여준다.

## 2-1-1. 딜/서폿 판정

바드·도화가·발키리·홀리나이트는 같은 직업이라도 딜과 서폿이 갈린다. **클래스만 보고
정하면 틀린다.** `synergy.ts`의 `resolveRole`이 두 신호를 순서대로 본다.

**1순위 — 직업 각인** (가장 안정적)

| 직업 | 서폿 | 딜 |
|---|---|---|
| 도화가 | 만개 | 회귀 |
| 바드 | 절실한 구원 | 진실된 용맹 |
| 발키리 | 해방자 | 빛의 기사 |
| 홀리나이트 | 축복의 오라 | 심판자 |

**2순위 — 아크패시브 진화 노드** (각인을 모르는 직업만)

서폿은 진화 2티어 `축복의 여신`, 3티어 `정열의 춤사위`를 찍는다.

**진화 노드를 1순위로 두면 안 된다.** 솔플하려고 진화만 딜로 바꿔두는 경우가 흔하다.
각인은 폿인데 세팅만 딜인 캐릭터가 실제로 있고(동네콩), 그런 캐릭터는 레이드에 폿으로
간다. 각인이 그 캐릭터가 무엇으로 레이드를 가는지를 나타낸다.

딜 세팅을 한 서폿 직업은 **서폿 버프 시너지를 주지 않으므로** 시너지 계산에서도 뺀다.
수동 역할 지정 UI는 없다. 판정이 데이터에서 나오므로 손댈 일이 없다.

## 2-1-2. 아크그리드 코어 단계

`질서 222 · 혼돈 111`의 그 숫자다. **코어 이름에 단계가 붙어 있고, API는 그 매핑을
주지 않는다.** 확인해본 것들(같은 길을 다시 파지 않는다):

| 후보 | 결과 |
|---|---|
| 젬 포인트 | 질서(20·18·20)와 혼돈(18·20·20)이 거의 같은데 222와 111로 갈린다 |
| 등급 | 여섯 코어가 모두 `고대`인데도 다르다 |
| 코어 공급 의지력 | 등급만 따라간다(유물 15, 고대 17) |
| 아이콘 | 해/달/별 위치만(96~101 고정) |
| 코어 옵션 수치 | 각인마다 달라 기준이 못 된다 |

그래서 `src/lib/arkGridCores.ts`에 이름→단계 표를 둔다. **30개 직업 540개 코어를
모두 채웠다.**

출처는 인벤 "전직업 아크 그리드 효과 정리"(`board/lostark/4821/107400`)와 그 댓글이
가리키는 직업별 글이다. 직업별 글은 `<해1> 티거 미스트리스`처럼 단계를 직접 적어 둔다.
혼돈 코어는 직업 공용이라 글에 단계가 적혀 있지 않다. **대표 글의 위치별 나열 순서가
곧 번호(1~6)다.** 순서를 바꾸면 전 캐릭터의 표기가 어긋나므로 그대로 유지한다.

패치로 코어가 늘면 같은 글을 다시 보고 채운다. loawa의 `/stat/equiparkgrid`도 같은
정보를 보여주지만 브라우저에서 그려져 자동 수집이 안 된다.

**단계는 저장하지 않고 표시할 때 찾는다.** 표에 이름을 추가하면 캐릭터를 다시
조회하지 않아도 바로 반영된다. 표에 없는 코어는 `?`로 두고, 여섯 개를 모두 모르면
숫자 대신 등급 구성(`고대6`)을 보여준다. 틀린 숫자를 보여주지 않는다.

## 2-1-4. 시너지 중첩

게임 규칙이라 추측하지 않는다. 사용자가 확인해 준 값이다.

| 경우 | 중첩 |
|---|---|
| 같은 직업 + 같은 각인 | **안 된다.** 둘이 들어와도 한 번만 센다 |
| 같은 직업 + 다른 각인 | 된다 |
| 다른 직업 + 같은 종류 | 된다 |

중첩을 가르는 열쇠는 사람이 아니라 **직업과 각인의 조합**이다. 그래서 `partySynergies`는
`classEngraving`까지 받는다. 파티 요약의 수치는 중첩되는 것만 더한 값이다
(치적 10% 둘이 다른 직업이면 20%, 같은 직업+각인이면 10%).

## 2-1-3. 시너지는 트라이포드에서 읽는다

`synergy.ts`의 클래스 표는 **"이 직업은 보통 이걸 준다"까지만** 안다. 실제로는 그
트라이포드를 찍었을 때만 나오고, 딜 발키리처럼 직업만으로는 답이 안 나오는 경우도 있다.

**툴팁이 시너지를 문장으로 그대로 말해준다.** 실측한 문구다.

```
건슬링어 AT02 유탄 / 급소 노출
  "자신 및 파티원에게 받는 치명타 저항률이 8.0초간 10.0% 감소한다"  → 치적 10%
발키리 전진 찌르기 / 약점 공략
  "자신 및 파티원의 치명타 공격에 받는 피해가 8.0% 증가한다"        → 치피증 8%
서머너 이끼 늪 / 부식성 확산
  "적중된 적의 모든 방어력을 16.0초간 12.0% 감소 시킨다"            → 방깍 12%
```

수치가 클래스 표의 상수와 정확히 일치한다. 등록된 캐릭터 6명으로 확인했고, 표에 답이
있던 5명은 값까지 같았다.

`armory.ts`의 `normalizeSkillSynergies`가 읽는다. 조심할 것이 다섯이다.

| | |
|---|---|
| **대상이 둘로 갈린다** | 파티 버프는 `파티원`이라고 말하지만 **방깍은 그러지 않는다.** `파티원`으로 거르면 방깍 직업이 통째로 빠진다 |
| 자기 깎아먹기 | `자신의 방어력이 감소`도 같은 문구다. 방깍은 대상과 효과 사이에 `자신`이 끼면 끊는다 |
| **받피증에는 그 가드를 붙이지 않는다** | `대상이 자신 및 파티원에게 받는 피해가 증가`라 사이에 `자신`이 늘 낀다. 붙였다가 받피증이 통째로 빠졌다(cd988a6 → 66b94dc) |
| 지속시간이 중간에 낌 | `저항률이 6.0초간 10.0% 감소`. `[^.]`로는 못 건너뛴다(6.0에도 점이 있다) |
| 문구가 서로를 품음 | 치피증 문구에 `받는 피해가 … 증가`가 들어 있다. **좁은 규칙을 앞에 둔다** |

그래서 규칙마다 대상을 직접 물고 있다. 툴팁 하나를 통째로 거르는 관문은 두지 않는다.

**받피증은 같은 효과를 세 가지 말투로 적는다.** 어느 쪽에서 보느냐의 차이뿐이라 셋 다
받피증으로 읽는다.

```
브레이커 피해 증폭    "대상이 자신 및 파티원에게 받는 피해가 6.0% 증가"
도화가   낙인 강화    "6.0초간 적이 받는 피해가 10.0% 증가"
바드     음표 낙인    "자신 및 파티원이 주는 피해가 10.0% 증가"
```

적을 무는 규칙은 **조사까지 요구한다.** `적`만 물면 `적중`에 걸려서 호크아이 최후의
표적(`적중된 대상에게 … 호크아이에게 받는 피해가 증가`)처럼 자기만 받는 버프를
시너지로 읽는다.

`Character.skillSynergies`에 저장한다. **null과 빈 배열이 다른 뜻이다.**

| 값 | 뜻 | 동작 |
|---|---|---|
| `null` | 스킬을 받아본 적이 없다(옛 데이터) | 클래스 표로 떨어진다 |
| `[]` | 받아봤는데 안 찍었다 | 시너지 없음 + 경고 |
| `[...]` | 찍었다 | 그대로 쓴다 |

서폿 버프는 트라이포드가 아니라 직업에서 나오므로 언제나 클래스 표를 따른다.

### 서포터는 아크패시브 노드도 읽는다

**서포터의 딜 시너지는 트라이포드에 없다.** 직업 각인 노드가 통째로 들고 있어서,
트라이포드만 보면 서포터는 늘 빈손으로 나온다.

```
도화가 깨달음 4티어 낙인 강화   "적이 받는 피해가 10.0% 증가"              → 받피증 10%
발키리 깨달음 4티어 해방자의 흔적 "자신과 파티원이 주는 피해가 10.0% 증가"  → 받피증 10%
도화가 깨달음 1티어 회귀        "먹물 낙인이 적의 방어력을 12.0% 감소"      → 방깍 12%
```

`normalizeArkPassiveSynergies`가 읽고, **역할이 서폿일 때만** `characters.ts`가 섞는다.
같은 종류가 양쪽에 다 있으면 트라이포드가 이긴다. 노드 툴팁은 코어 툴팁과 형식이 달라
설명이 `MultiTextBox` 하나에 통째로 들어간다.

**딜러에는 섞지 않는다.** 딜러는 트라이포드가 답이고, 깨달음 노드에는 자버프 문장이
길게 섞여 있어 잘못 읽을 여지만 늘어난다. 대신 딜바드(진실된 용맹)처럼 각인이 낙인을
방깍으로 바꾸는 경우는 트라이포드에 나오지 않아 지금도 놓친다.

**도약에는 없다.** 등록 캐릭터 30명을 훑어봤고 도약은 1티어가 각성기 공용,
2티어가 직업별 스킬 강화뿐이었다. 같은 길을 다시 파지 않는다.

**마나는 트라이포드에서만 나온다.** 서머너 슈르디의 `마나 회복`처럼 골라 찍는 것이라
"이 직업은 보통 준다"고 말할 근거가 없어 클래스 표에 넣지 않는다. 딜 시너지가 아니라
유틸이므로 커버리지(`TRACKED_KINDS`)에서도 뺀다 — 빠졌다고 편성이 틀린 것이 아니다.
찍고 온 사람이 있으면 그 칸과 파티 요약에 그대로 나온다.

확인하지 못한 종류(공증·방깍·백헤드)는 추정 문구로 뒀다. 안 잡히면 클래스 표로
떨어지므로 지금보다 나빠지지 않는다. 해당 직업으로 확인되면 문구를 고쳐 넣는다.

## 2-2. 입력은 칸에서 한다

**편성 칸에 닉네임을 치면 조회·등록·배치가 한 번에 끝난다.** 시트에서 셀에 닉네임을 적던
것과 같은 동작이고, 이게 이 앱의 주 입력 경로다(`assignByName`).

- 이미 등록된 캐릭터면 API를 부르지 않는다. 분당 100회 한도를 아낀다
- 없는 닉네임이면 그 자리에서 로아 API로 조회해 `Character` 행을 만든다
- **"먼저 캐릭터를 등록하고 그다음 배치"라는 단계를 사용자에게 만들지 않는다.**
  `Character` 테이블은 스펙 캐시일 뿐 사용자가 의식할 대상이 아니다
- `/characters`는 입구가 아니라 **정리용 화면**이다. 오타 캐릭터 삭제, 딜/서폿 교정,
  스펙 갱신, 부캐 묶기처럼 칸 안에서 하기 좁은 일만 여기서 한다

## 2-3. 숙제는 편성표에서 나온다

**따로 체크하는 목록을 만들지 않는다.** 편성 칸에 넣는 순간 요일·시각이 정해지므로,
그 시각이 지나면 다녀온 것으로 본다(`homework.ts`의 `raidPassed`).

손으로 켜고 끄는 목록을 하나 더 두면 편성표와 어긋나는 순간부터 아무도 믿지 않는다.
시트를 대체하려던 이유가 바로 그 "두 곳에 같은 것을 적는" 문제였다.

숙제 화면은 **내 캐릭터만** 보여준다. 길드 전체를 늘어놓으면 무엇이 내 일인지 찾는
화면이 되고, 골드 합계도 남의 것과 섞여 의미가 없어진다. 내 것을 가리는 기준은 소속이라,
캐릭터 관리에서 원정대를 불러온 적이 없으면 화면이 비어 있다.

### 골드 표

`src/lib/raidRewards.ts`가 유일한 출처다. 로아 API에는 레이드 목록도 난이도도 없으니
보상은 더더욱 없다(3.3-1).

**사용자가 쓰는 골드 계산기 화면에서 관문별로 확인한 값만** 넣는다. 다른 사이트의 표를
옮겨 두지 않는다. 실제로 두 군데가 어긋났고(종막 하드 52,000→48,000, 세르카 노말
35,000→32,000), 섞여 있으면 어느 값이 확인된 것인지 알 수 없다.

관문 값도 합쳐서 **레이드 단위 합계만** 둔다. 편성표가 슬롯(레이드) 단위라 관문을 따로
셀 근거가 화면 어디에도 없다. PC방 더보기도 다루지 않는다. 자리에 따라 달라지는 값이라
편성표가 알 수 없다.

확인하지 못한 난이도는 **아예 두지 않는다.** 표에 없으면 화면이 `-`를 찍는다.
모르는 값을 채워 넣으면 합계가 조용히 틀어져 "이번 주에 얼마 벌었나"를 잘못 알려준다.
빈칸보다 나쁘다.

### 골드 한도는 둘이다

게임 규칙이라 추측하지 않는다. 사용자가 확인해 준 값이다. 둘이 서로 다른 것을 센다.

| 한도 | 단위 | 넘기면 |
|---|---|---|
| **6명** (`GOLD_LIMIT`) | 원정대 | 골드를 못 받는 대신 **더보기가 무료**다 |
| **3개 레이드** (`RAID_GOLD_LIMIT`) | 캐릭터 | 골드만 0이고 **더보기 비용은 그대로 나간다** |

둘의 더보기 취급이 다르다. 위는 골드를 아예 못 받는 캐릭터고, 아래는 골드를 받는
캐릭터가 네 번째 레이드에 간 것이다. 값을 치르는 쪽과 아닌 쪽이 갈린다.

### 어느 셋이 골드를 받는가 — 순서가 정한다

**게임에서 사람이 고르는 값이라 앱이 대신 정하지 않는다.** 그래서 숙제 화면의 줄
순서가 곧 답이고, 앞에서부터 셋만 골드를 받는다. 번호 뱃지를 끌어 옮기면 바뀐다.

기본은 **보상이 큰 순**이다. 손대지 않아도 그 주의 최대가 되는 쪽이어야 한다. 요일
순으로 두면 늦은 요일에 잡힌 큰 레이드가 이유 없이 잘려 나간다. 요일은 줄마다
뱃지로 그대로 보이므로 일정을 읽는 데 지장이 없다.

옮긴 순서는 `Assignment.homeworkOrder`에 박는다. **한 캐릭터의 그 주 배정 전체에
0부터 박고**, 그 뒤에 새로 들어온 배정만 null로 남아 맨 뒤에 붙는다. 나중에 온
레이드가 이미 정해 둔 골드 자리를 조용히 뺏지 않게 하려는 것이다. 승계는 이 값을
복사하지 않으므로 주차가 바뀌면 다시 보상 순이다.

보상을 모르는 레이드(표에 없음)는 아는 것들 뒤에 선다. 아는 값이 모르는 값에 밀려
0이 되는 일은 없다.

**규칙은 `src/lib/homeworkOrder.ts` 한곳에 있고 서버와 화면이 함께 쓴다.** 화면은
끌어 놓는 순간 서버를 기다리지 않고 골드를 다시 그려야 하는데(그러지 않으면 바꾼
결과가 한 박자 늦게 와서 무엇이 달라졌는지 알 수 없다), 두 곳이 따로 계산하면 같은
줄에 다른 골드가 찍힌다. 그래서 이 파일은 `server-only`가 아니다. 유닛 테스트가
붙어 있다.

**순서를 바꾸는 것은 내 캐릭터뿐이다.** 편성은 누구나 손대지만(4장) 이건 편성이 아니라
남의 골드 계산이고, 남이 바꿔 봐야 그 사람 화면에는 보이지도 않는다.

## 3. 규칙

### 3.1 절대 커밋 금지

```
.env, .env.local
/src/generated
/.probe
node_modules/
```

- **로아 API 키(`LOSTARK_API_KEY`)를 코드·주석·문서·테스트 픽스처 어디에도 하드코딩하지
  않는다.** 예시는 `YOUR_LOSTARK_JWT_HERE` 같은 플레이스홀더로
- **`NEXT_PUBLIC_` 접두사를 API 키에 붙이지 않는다.** 붙는 순간 브라우저 번들로 새어
  나간다. 로아 공식 문서도 서버 보관을 권장한다
- 키는 `src/lib/lostark.ts`를 통해서만 읽는다. 이 파일은 브라우저 실행을 런타임 가드로 막는다
- 실수로 커밋된 키는 코드 수정으로 해결되지 않는다. 히스토리에 남으므로 **개발자 포털에서
  클라이언트를 삭제하고 재발급**해야 한다. 발견 시 즉시 사용자에게 알린다

### 3.2 로아 API 사용

- **분당 100회.** **키(클라이언트)마다** 따로 센다. `src/lib/lostark.ts`의 큐가 여유 있는
  키를 골라 주고 429를 백오프로 재시도한다. 이 래퍼를 우회해 `fetch`를 직접 부르지 않는다
- 키를 여러 개 쓰려면 `LOSTARK_API_KEYS`에 쉼표로 나열한다. 동시에 나가는 요청 수가
  키 수와 같아지므로 전체 갱신이 그만큼 빨라진다. 하나만 있으면 예전처럼 한 번에 하나씩이다
- **캐릭터 하나당 요청 1회.** `?filters=profiles+engravings+arkgrid+arkpassive+combat-skills`로 묶어서 받는다.
  세 엔드포인트를 따로 부르면 한도가 3배로 준다
- 응답 수치는 `"1,770.83"`처럼 **천 단위 쉼표가 붙은 문자열**로 온다.
  반드시 `parseNumeric()`을 거친다. `Number()`를 직접 쓰면 `NaN`이 된다
- 조회 실패는 `Character.syncError`에 남기고 **기존 스펙 값은 지우지 않는다.**
  시트의 `#REF!`처럼 조용히 비는 상황을 다시 만들지 않는다

### 3.3 응답 구조 (2026-09-04 실측 확정)

추측이 아니라 `npm run probe`로 확인한 값이다. 게임 개편으로 바뀌면 프로브부터 다시 돌린다.

| 항목 | 실제 위치 | 비고 |
|---|---|---|
| 템레벨 | `ArmoryProfile.ItemAvgLevel` | **`ItemMaxLevel`은 더 이상 오지 않는다** |
| 전투력 | `ArmoryProfile.CombatPower` | `"5,043.42"` 문자열. 존재 확인됨 |
| 이미지 | `ArmoryProfile.CharacterImage` | `cdn-lostark.game.onstove.com` |
| **직업 각인** | `ArkGrid.Slots[].Tooltip`의 "코어 옵션 발동 조건" | 아래 설명 참조 |
| 전투 각인 | `ArmoryEngraving.ArkPassiveEffects` | 원한·예리한 둔기 등. **직업 각인이 아니다** |
| 아크그리드 | `ArkGrid.Slots` (코어 6), `ArkGrid.Effects` | 질서/혼돈 × 해/달/별 |
| 원정대 | `/characters/{name}/siblings` | 여기도 `ItemAvgLevel`뿐 |

**직업 각인은 아크그리드 코어의 발동 조건에서 읽는다.**

```
ArkGrid.Slots[].Tooltip → ItemPartBox "코어 옵션 발동 조건"
  "수라의 길 전용아크 패시브 4티어 무아지경 활성화 필요"
   ^^^^^^^^^ 여기가 직업 각인
```

아크그리드 코어는 직업 각인 전용으로 나오기 때문에 조건 문구가 그 이름으로 시작한다.
`armory.ts`의 `readCoreClassEngraving`이 이걸 읽는다. **캐릭터가 실제로 찍은 각인**이
나오므로 같은 직업이라도 갈린다(스카우터: 아르데타인의 기술 / 진화의 유산).

깨달음 노드만 보고 알아내려던 시도는 모두 실패했으니 되풀이하지 않는다.

| 시도 | 결과 |
|---|---|
| 티어로 구분 | 불가. 건슬링어 1티어, 소울이터 2티어, 버서커 4티어 |
| 툴팁 구조로 구분 | 불가. 일반 노드와 요소 구성이 같다 |
| 아이콘 번호로 구분 | 불가. 규칙이 없다 |
| 아이콘 아틀라스로 구분 | 직업 전용 노드(3~6개)까지만 좁혀진다 |

`src/lib/classEngravings.ts`의 이름표는 **아크그리드를 아직 끼지 않은 캐릭터**를 위한
대비책일 뿐이다. 표에 없으면 비워둔다.

`ArmoryEngraving.ArkPassiveEffects`는 이름과 달리 **전투 각인**(원한, 예리한 둔기)이다.
헷갈리기 쉬우니 주의한다.

`Description`이 태그가 섞인 고정 형식이라 파싱해서 쓴다.

```
<FONT color='#83E9FF'>깨달음</FONT> 1티어 <FONT color='#83E9FF'>수라의 길 Lv.1</FONT>
→ 태그 제거 → "깨달음 1티어 수라의 길 Lv.1" → { category, tier, name, level }
```

브레이커·도화가·소서리스·데빌헌터 4개 클래스로 확인했다.
조회는 `?filters=profiles+engravings+arkgrid+arkpassive+combat-skills`로 **여전히 요청 1회**다.

**툴팁은 저장하지 않는다.** 아크그리드 응답 87KB 중 대부분이 코어·젬의 Tooltip 문자열이고,
걷어내면 1KB 미만이 된다. 게임 내 표시용 마크업이라 이 앱이 쓸 일이 없다.
정규화는 `src/lib/armory.ts`가 전담하며 유닛 테스트가 붙어 있다.

### 3.3-1 레이드·난이도 목록은 API에 없다 (2026-09-05 실측)

`RAID_PRESETS`를 API로 대체할 수 있는지 확인했다. **없다. 하드코딩을 유지한다.**

실제로 응답이 오는 경로는 이게 전부다.

| 경로 | 주는 것 |
|---|---|
| `/news/notices`, `/news/events` | 공지·이벤트 목록 |
| `/markets/options` | 거래소 아이템 분류(장비 상자·각인서·강화 재료 …) |
| `/gamecontents/calendar` | 시간표가 있는 월드 콘텐츠 |
| `/armories/...`, `/characters/{name}/siblings` | 캐릭터 스펙 |

`/gamecontents/calendar`가 이름만 보면 답 같지만 **군단장·에픽 레이드가 없다.**
들어 있는 분류는 모험 섬·섬·태초의 섬·카오스게이트·필드보스·항해·로웬뿐이고,
`MinItemLevel`은 있어도 난이도(노말/하드/나이트메어) 개념 자체가 없다.

`/gamecontents/challenge-abyss-dungeons`와 `challenge-guardian-raids`는 문서에 있던
경로지만 지금은 **HTML(문서 페이지)이 온다.** 없어진 경로다. 상태 코드가 200이라
`res.ok`만 보면 속으니, 새 경로를 뚫을 때는 본문이 `<`로 시작하는지 확인한다.

그래서 `src/lib/raids.ts`의 프리셋이 유일한 출처다. 어차피 `raidName`은 자유 입력이라
신규 레이드가 나와도 앱 수정 없이 쓸 수 있고, 프리셋은 자주 쓰는 것을 위로 올려주는
편의일 뿐이다.

### 3.4 경고는 차단하지 않는다

템레벨 미달, 같은 레이드 중복, 서폿 자리의 딜러 클래스는 **경고만 띄우고 배치는 허용한다.**
시트도 강제하지 않았고 예외 상황이 늘 있다. 검증을 하드 블로킹으로 바꾸지 않는다.

### 3.4-1 주간 제한 규칙

게임 규칙이라 추측하지 않는다. 사용자가 확인해 준 값이다.

**제한은 하나뿐이다. 한 캐릭터는 같은 레이드를 난이도가 달라도 한 주에 한 번만 간다.**

서로 다른 레이드는 개수 제한이 없다. "주 3개까지"로 알고 경고를 넣었다가 되돌렸으니
다시 넣지 않는다.

**캐릭터 단위다.** 사람(원정대) 단위가 아니다. 부캐를 바꿔 같은 레이드에 또 가는 것은
정상이고 실제로 그렇게 돌린다. 사람 단위로 세면 정상 편성에 경고가 붙는다.

난이도를 빼고 `raidName`만으로 센다. 벨가르딘 하드를 갔으면 노말은 못 간다.
`board.ts`의 `getBoard`가 주 전체를 보고 계산하므로 요일이 달라도 잡힌다.

### 3.5 커밋은 물어보지 않고, 푸시는 하지 않는다

**작업 하나가 끝나면 커밋까지 마치는 것이 기본이다.** 별도 요청이나 확인을 기다리지
않는다. 사용자가 "커밋하지 마"라고 말한 경우에만 멈춘다.

**푸시는 하지 않는다.** 커밋까지만 하고 멈춘다. `git push`는 사용자가 그 자리에서
직접 요청했을 때만 실행한다. 푸시하면 Vercel이 바로 배포해 길드원이 쓰는 화면이
바뀌므로, 무엇이 언제 나갈지는 사용자가 정한다.

**작업 하나**의 기준은 "지금 하던 것이 동작하는 상태"다. 로드맵 항목 하나, 버그 수정 하나,
리팩터 하나. 여러 관심사를 한 커밋에 섞지 않는다.

푸시할 때의 대상은 `origin main`이다. 1인 개발이라 브랜치를 따로 파지 않는다.

#### 커밋 전 체크

1. `git status`로 **3.1의 금지 목록이 스테이징되지 않았는지 확인한다.**
   `.env*`, `/src/generated`, `/.probe`, `node_modules/`
2. `git diff --staged`로 API 키·DB 접속 문자열이 섞여 들어가지 않았는지 본다
3. 테스트가 있는 영역을 건드렸으면 `npm test`. 특히 `src/lib/week.ts`는 필수(섹션 8)
4. **깨진 상태로 커밋하지 않는다.** 테스트나 타입 체크가 실패하면 고치고 커밋한다.
   단, 새 라우트 직후의 `tsc --noEmit` 실패는 6-1의 알려진 현상이니 빌드를 먼저 돌린다
5. `git commit --no-verify`로 훅을 건너뛰지 않는다. 훅이 막으면 원인을 고친다

`AGENTS.md`의 `nextjs-agent-rules` 블록은 `next dev`가 다시 써넣는다.
지워봐야 되살아나므로 변경분이 생겼으면 함께 커밋한다.

#### 메시지 형식

Conventional Commits 접두사 + **한국어 제목**. 제목은 50자 안쪽, 마침표 없이.

```
feat: 고정 요일표 CRUD 추가

요일·시간·레이드·공대장을 편집할 수 있게 했다.
Assignment는 건드리지 않으므로 주간 리셋 구조에 영향이 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

| 접두사 | 쓰는 경우 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 동작 그대로 구조만 변경 |
| `chore` | 의존성·설정·스크립트 |
| `docs` | 문서. CLAUDE.md 갱신 포함 |
| `test` | 테스트만 |

본문에는 **무엇을 했는지가 아니라 왜 그렇게 했는지**를 적는다. 무엇을 했는지는 diff에 있다.
자명한 변경이면 본문 없이 제목만으로 끝낸다.

---

## 4. 인증 — 디스코드 길드 멤버십

**2026-09-05에 방향을 바꿨다.** 그 전까지는 로그인을 만들지 않고 인스턴스 입장 암호로
가려 했는데, 길드가 이미 디스코드를 운영하고 있어 그쪽이 모든 면에서 낫다.
아래 "왜 암호가 아닌가"를 읽지 않고 암호 게이트로 되돌리지 않는다.

### 지금 — 디스코드 로그인 **필수**. 이미 붙어 있다

Discord OAuth2로 입장부터 막는다. 길드 디스코드 서버의 멤버가 아니면 편성표를 볼 수 없다.

- 스코프는 `identify` + `guilds.members.read`. 길드 ID로 멤버십을 확인한다
- 통과하면 서명 쿠키를 굽는다. `INSTANCE_SESSION_SECRET`을 그대로 쓴다
- **관문은 `requireSession`이다**(`src/lib/session.ts`). proxy로 막지 않는다.
  Next 문서가 proxy를 완전한 인가 수단이 아니라고 못 박기 때문이다
- 세션에 `discordUserId`가 들어 있다. 사람을 특정하는 키가 이것뿐이라 화면 설정도
  여기에 붙는다(아래)
- `Instance.passwordHash`와 관련 코드는 **쓰지 않는다.** 지우는 것은 실제로 대체한 뒤에

편집자 이름(`Assignment.createdByLabel`·`ChangeLog.actorLabel`)은 세션의 길드 닉네임이다.
폼에서 받지 않는다. 받으면 아무 이름이나 적어 보낼 수 있어 기록이 의미를 잃는다.

### 화면 설정 — 쿠키가 앞, DB가 뒤

테마와 편성표 보기(간략/전체)는 사람마다 다르다. `UserSetting`(키: `discordUserId`)에
저장하되 **화면은 언제나 쿠키를 읽는다.**

| | 쿠키만 | DB만 | 지금 방식 |
|---|---|---|---|
| 렌더 비용 | 없음 | 매 렌더 조회 | 없음 |
| 깜빡임 | 없음 | 없음 | 없음 |
| 기기 간 | 안 됨 | 됨 | **로그인할 때 넘어옴** |
| 로그인 전 | 됨 | 읽을 사람이 없음 | 됨 |

바꾸는 순간 쿠키를 쓰고 같은 값을 DB에도 적는다. **DB 값이 쿠키로 넘어오는 지점은
로그인 콜백 한 곳뿐이다**(`api/auth/discord/callback`). 이 구조를 "매 렌더 DB 조회"로
바꾸지 않는다. 페이지마다 순차 쿼리가 이미 여럿이다(섹션 5의 지역 선택 이유와 같다).

저장 실패는 알리지 않는다. 화면은 이미 바뀌어 있고 다음 로그인에서 옛 값이 돌아올 뿐이다.

### 왜 암호가 아닌가

| | 입장 암호 | 디스코드 |
|---|---|---|
| 공유·순환 | 사람이 관리 | 필요 없음 |
| 링크 유출 | 암호도 같이 샌다 | 멤버가 아니면 못 들어옴 |
| 길드 탈퇴 | 수동으로 암호 교체 | **자동으로 차단** |
| 편집자 이름 | 여전히 자기 신고 | 검증된 계정 |

### 편집은 계속 열려 있다 — 하드 블로킹 아니다

**입장한 사람은 누구의 캐릭터든 배치·이동·삭제할 수 있다.** 공대장이 대신 넣어주는 일이
흔하고, §3.4의 "경고만 띄우고 막지 않는다"가 여기에도 걸린다.

로그인은 *누가 했는지를 알기 위한 것*이지 *무엇을 못 하게 하려는 것*이 아니다.
"본인 캐릭터만 배치 가능" 같은 제약을 넣지 않는다.

### Member 묶기 — 이게 진짜 이득이다

`Discord 유저 ─1:1─ Member ─1:N─ Character`로 잇는다. `Member`에 `discordUserId`가 붙는다.

**주간 제한 경고는 Member와 상관이 없다.** 3.4-1에 적은 대로 제한이 캐릭터마다
걸리므로 `memberId`가 없어도 그대로 돈다. 예전에는 사람 단위로도 셌는데 그건 틀린
규칙이었다. 부캐를 바꿔 같은 레이드를 또 가는 것은 정상이고 실제로 그렇게 돌린다.

그래서 Member 묶기의 이득은 경고가 아니라 **누구의 캐릭터인지 알아보는 것**이다.
캐릭터 관리 화면이 사람별로 묶여 보이고, 원정대 단위로 한 번에 지울 수 있다.

### 소속은 어디서 등록했느냐로 갈린다

| 등록 경로 | 소속 |
|---|---|
| 캐릭터 관리 › 내 원정대 불러오기 | **등록한 사람** |
| 캐릭터 관리 › 한 명씩 등록 | **등록한 사람** |
| 편성표 칸에 닉네임 입력 | **무소속** (단, 누가 이미 자기 것으로 등록해 뒀으면 그 사람) |

캐릭터 관리는 자기 캐릭터를 챙기는 화면이고, 편성 칸은 **남의 캐릭터를 대신 넣는 자리**다.
칸에 치는 사람으로 묶으면 남의 부캐가 전부 공대장 소속이 된다.

등록하면 이름이 `Member.claimedNames`에 쌓인다. **그 뒤로 같은 이름이 등록되면 자동으로
붙는다.** 이게 없으면 남이 편성 칸에 대신 넣어줄 때마다 소속이 비어 매번 다시 등록해야 한다.

계정이 여럿인 사람은 **계정마다 한 번씩** 불러오면 된다. 이름이 계속 쌓이는 구조라 그대로
동작한다. 이 구조를 "원정대 하나 = 사람 하나"로 단순화하지 않는다.

### 칸의 자동완성

| 상태 | 보여주는 것 |
|---|---|
| 아무것도 안 침 | **내 캐릭터만** (템레벨 내림차순) |
| 한 글자 이상 침 | 그 글자에 맞는 전체 캐릭터 |
| 언제나 | 같은 레이드에 이미 들어간 캐릭터는 뺀다 |

칸을 누르는 사람은 대개 자기 캐릭터를 넣는다. 길드 전체 목록을 먼저 들이밀면 그 안에서
자기 것을 찾아야 한다. 남의 캐릭터를 대신 넣을 때는 이름을 알고 있으므로 한 글자면 된다.

`Member.discordUserId`는 유니크가 아니라 인덱스다. 이미 데이터가 있는 표에 유니크를
얹으려면 `--accept-data-loss`가 필요한데, 클레임은 사람당 한 번이라 코드에서 막는 편이 싸다.

**여러 계정을 쓰는 사람이 있다.** 로아 `siblings`는 한 계정의 원정대만 주고 부계정을
이어붙일 방법이 API에 없다. 그래서 대표캐릭터명을 한 번 더 받아 **같은 Member에 원정대를
추가로 묶는다.** 계정이 몇 개든 대응된다. 이 구조를 1:1 원정대로 단순화하지 않는다.

### 못 하는 것

**디코 닉은 소유 증명이 아니다.** 로아 API에 "이 캐릭터가 누구 것인가"가 없어서 첫
클레임은 선착순 자기 신고다. 남의 원정대를 먼저 클레임할 수 있고 이건 어떻게 붙여도
못 막는다. 신뢰가 "익명"에서 "검증된 디코 계정"으로 오를 뿐이다.

### 비밀 취급

`DISCORD_CLIENT_SECRET`은 `LOSTARK_API_KEY`와 같은 등급으로 다룬다. §3.1이 그대로 적용된다.
**`NEXT_PUBLIC_`을 붙이지 않는다.** 클라이언트 ID는 공개돼도 되지만 시크릿은 아니다.

---

## 5. 현재 상태 (2026-09-04 기준)

### 확정된 것

| 항목 | 값 |
|---|---|
| 스택 | Next.js 16 (App Router) + React 19 + Tailwind 4 |
| DB | Postgres. Prisma 7 + `@prisma/adapter-pg` |
| 배포 | **Vercel + Neon. 돌고 있다.** https://loa-raid.vercel.app |
| 지역 | **둘 다 싱가포르.** Neon `aws-ap-southeast-1`, Vercel `sin1` |
| 테스트 | vitest |

**지역을 싱가포르로 고정한 이유**: 페이지 렌더링마다 DB 쿼리가 여러 번 순차로 나가므로
함수와 DB가 같은 지역에 있어야 한다. Neon은 한국·일본에 지역이 없어 싱가포르가 최선이고,
서울(`icn1`)에 함수를 두면 쿼리마다 태평양을 왕복해 오히려 느려진다.
Vercel 지역은 `vercel.json`에 박아뒀다. **Neon은 생성 후 지역 변경이 불가능하다.**

### 완료

- 저장소 정리(Gmail 코드 전부 삭제) 및 Next.js 스캐폴드
- `prisma/schema.prisma` — 전체 데이터 모델
- `src/lib/week.ts` — 주차 계산 + 유닛 테스트 14개
- `src/lib/synergy.ts` — 30개 클래스 시너지·역할 매핑 (트라이포드가 이긴다, 2-1-3)
- `src/lib/lostark.ts` — API 클라이언트 (레이트리밋, 429 백오프)
- `src/lib/positions.ts`, `src/lib/raids.ts`, `prisma/seed.mts`
- `src/lib/armory.ts` — API 응답 정규화 (직업 각인·전투 각인·아크그리드·템레벨·전투력)
- **로아 API 연동 실동작 확인.** 실제 캐릭터로 조회·정규화까지 통과했다
- **캐릭터 관리 화면.** 단일 등록, 원정대 조회 후 선택 등록, 갱신, 역할 전환, 삭제.
  API→정규화→DB→화면 전 경로를 실제 캐릭터로 확인했다
- **고정 요일표 편집** — 슬롯 추가·수정·보관
- **편성표** — 요일 탭, 주차 이동, 칸 인라인 입력, 시너지 요약, 경고, 자리/레이드 고정.
  승계 동작(고정만 넘어옴, 지운 자리는 되살아나지 않음)까지 실측으로 확인했다
- **테마** — 라이트/다크/시스템 3단 토글. **쿠키에 저장한다.** 서버가 첫 HTML의
  `<html data-theme>`을 직접 박아 번쩍임을 막으므로 인라인 스크립트가 없다.
  React 트리 안의 `<script>`는 클라이언트에서 다시 그려질 때(router.refresh 등)
  "스크립트는 실행되지 않는다"는 콘솔 오류를 낸다. 스크립트 방식으로 되돌리지 않는다
- 아크그리드는 포인트가 아니라 **코어 단계**(질서 333 · 혼돈 333)로 보여준다.
  임계값 `[10,14,17]`을 코어 툴팁에서 읽어 계산한다
- **Neon DB 연결 완료** (싱가포르). 스키마 반영과 시드 실행까지 확인했다.
  기본 인스턴스 slug는 `main`이다

### 환경

`.env.local`에 `DATABASE_URL`(풀링), `DATABASE_URL_UNPOOLED`(직결), `LOSTARK_API_KEY`,
`INSTANCE_SESSION_SECRET`이 모두 채워져 있고 동작을 확인했다.

---

## 6. Prisma 7 주의사항

버전이 올라가며 바뀐 것들이라 옛 문서를 따라가면 막힌다.

- **접속 URL이 `schema.prisma`에 없다.** `prisma.config.ts`가 갖는다
- **`.env`를 자동으로 읽지 않는다.** `prisma.config.ts`가 `process.loadEnvFile()`로 직접 읽는다
- **런타임은 드라이버 어댑터로 접속한다.** `src/lib/prisma.ts`의 `PrismaPg`
- **Neon 접속 문자열은 두 개다.** 앱 런타임은 풀링(`DATABASE_URL`, 호스트에 `-pooler`),
  Prisma CLI의 스키마 작업은 직결(`DATABASE_URL_UNPOOLED`)을 쓴다. 풀러(PgBouncer)로는
  마이그레이션이 통과하지 못한다. 둘을 섞지 않는다
- 생성된 클라이언트는 `src/generated/prisma`에 나온다. 커밋하지 않으므로 클론 후
  `npm run db:generate`가 필요하다
- 생성 파일이 확장자 없이 import하면 node가 해석하지 못해 `prisma/seed.mts`가 깨진다.
  그래서 `importFileExtension = "ts"`를 켜뒀다. 이 옵션을 지우지 않는다

---

## 6-1. Next 16 주의사항

`AGENTS.md`가 가리키는 `node_modules/next/dist/docs/`가 이 버전의 정답지다.
훈련 데이터와 다른 부분이 있어 코드 쓰기 전에 확인한다.

- `params`와 `searchParams`는 **Promise**다. `const { slug } = await params`
- 페이지·레이아웃 props는 생성 타입 `PageProps<"/i/[slug]">`, `LayoutProps<"/i/[slug]">`를 쓴다.
  이 타입은 `next build`나 `next dev`가 만들므로, **새 라우트를 추가한 직후의
  `tsc --noEmit`은 실패한다.** 빌드를 한 번 돌린 뒤 다시 확인한다
- Turbopack이 기본이다. `--turbopack` 플래그가 필요 없다
- `middleware`는 `proxy`로 대체됐다. 디스코드 입장 게이트를 만들 때 확인한다
- `export const dynamic = "force-dynamic"`은 아직 유효하다(Cache Components를 켜면 제거된다).
  Prisma로 DB를 읽는 페이지에 붙여 빌드 시점 프리렌더를 막는다
- **`_`로 시작하는 폴더는 라우팅에서 제외된다**(private folder). `app/api/_foo/route.ts`는 404다

## 7. 로드맵

```
[x] 저장소 정리 + Next.js 스캐폴드
[x] 스키마, 주차 계산, 시너지 표, API 클라이언트
[x] DB 마이그레이션 + 시드 (Neon 싱가포르)
[x] 로아 API 프로브 → 각인·아크그리드 구조 확정 + 정규화
[x] 캐릭터 관리 (등록, 원정대 선택 등록, 동기화, Member 묶기)
[x] 딜/서폿 자동 판정 (아크패시브 진화 노드)
[x] 고정 요일표 CRUD
[x] 주간 배정 + ChangeLog
[x] 리셋 예외 (자리 핀, 레이드 토글)
[x] 경고와 시너지 요약
[x] 라이트/다크 테마 (쿠키 + UserSetting)
[x] 디스코드 로그인 게이트 (입장 검사 + 세션)
[x] 캐릭터 스펙 자동 갱신 + 전체 갱신(회차 분할)
[x] 편집 이력 화면
[x] 편성표 간략 보기 (8인 표)
[x] 4인 레이드
[x] 고정 현황 화면 (/i/[slug]/pinned)
[x] Member 묶기 (원정대 클레임)
[x] Vercel 배포
[x] 숙제 관리 (편성표에서 자동 판정 + 골드 표)
[x] 미정 요일 (혼자 도는 레이드를 숙제에만 올리기)
[x] 캐릭터당 주 3개 골드 한도 + 숙제 순서 끌어 옮기기
```

---

## 8. 작업 시 유의

- 커밋은 3.5를 따른다. 요청을 기다리지 않고, 커밋 전 `git status`로 `.env.local`을 확인한다.
  **푸시는 사용자가 직접 요청했을 때만 한다**
- `scripts/*.mts`는 node가 **타입만 벗겨서** 실행한다. 변환이 필요한 문법을 쓸 수 없다.
  파라미터 프로퍼티(`constructor(readonly x: T)`)와 enum이 대표적이다.
  또 이 스크립트가 닿는 모듈은 확장자 없는 런타임 import를 하면 안 된다
  (`import type`은 지워지므로 괜찮다)
- `src/lib/week.ts`를 고치면 반드시 `npm test`를 돌린다. 주차 경계는 눈으로 검증이 어렵다
- 사용자에게 터미널 출력 공유를 요청할 때 **API 키를 지우고 붙여넣도록 안내**한다
- 칸에 보여줄 각인은 **직업 각인**이다(`Character.classEngraving`). 전투 각인은 상세로 민다
- **정규화 형식을 바꾸면 저장된 데이터는 그대로다.** 저장 값은 조회 시점 형식으로 굳어 있어
  코드만 고쳐선 안 바뀐다. 캐릭터 관리 화면의 `전체 갱신`으로 되살린다.
  옛 형식도 화면이 깨지지 않게 읽는 쪽에 fallback을 둔다
- 캐릭터 초상은 **증명사진형 썸네일**이다(`Portrait.tsx`). 배경으로 깔지 않는다.
  전신을 칸에 맞추면 인물이 알아볼 수 없이 작아지고, 이미지의 어두운 배경이 카드와
  부딪혀 경계가 날카롭게 잘린다. 머리부터 상체까지 확대해 잘라내고 모서리를 둥글린다.
  이미지가 없으면 클래스 색 + 클래스명 두 글자로 대체한다
- **화면에 나가는 글은 존댓말이다**(`…합니다`, `…해 주세요`). `README.md`도 마찬가지다.
  저장소를 처음 보는 사람이 읽는 글이라 화면과 같은 말투를 쓴다.
  이 문서(CLAUDE.md)와 코드 주석은 지금 말투를 그대로 쓴다. 읽는 사람이 다르다.
  새 문구를 쓸 때 주석 말투를 UI나 README로 옮기지 않는다. 버튼·레이블 같은 명사와
  개발자만 보는 설정 오류는 예외다
- 이 문서와 실제 상태가 어긋나면 문서를 갱신한다. 특히 섹션 5와 7
